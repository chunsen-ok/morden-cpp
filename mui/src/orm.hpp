#ifndef MUI_ORM_HPP
#define MUI_ORM_HPP

namespace mui {

class QueryBuilder
{
public:
};

}

/*

QueryBuilder builder;
builder.table("").columns().where("").select(); // select ${columns} from ${table} where ${where};
builder.table("").values().insert(); // insert into ${table} () values ();

*/

#endif