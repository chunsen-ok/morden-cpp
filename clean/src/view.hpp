#ifndef CLEAN_VIEW_HPP
#define CLEAN_VIEW_HPP

#include <string>

class ChatItem
{
public:

private:
    void openFile()
    {
        /*
        const auto saved_path = store->value(RFileSavedPath);
        if (saved_path.empty()) {
            // download
            // ...
            fetcher->fetch_resource(url);
        } else if (saved_path.exists()) {
            DesktopServices::openUrl(saved_path);
        } else {
            // Show file's been cleared
        }
        */
    }

    std::string url;
};

#endif
